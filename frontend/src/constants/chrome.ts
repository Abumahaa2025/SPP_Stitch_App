/** Reserved space for the floating glass tab bar (safe area extra handled per screen). */
export const TAB_BAR_RESERVED = 96;

/** Chat input row sits just above the tab bar. */
export const CHAT_INPUT_BOTTOM = TAB_BAR_RESERVED + 12;

/** Calm brand-only header row — no quick actions or ask bar. */
export const WORKSPACE_BRAND_ONLY_HEIGHT = 56;
