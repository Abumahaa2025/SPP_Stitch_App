This file exists to document and validate OTA workflow path triggers.

The Expo OTA workflow runs on pushes to `main` when files under `frontend/**` change.

Last token validation: triggered after owner rotated EXPO_TOKEN in GitHub Actions secrets.
